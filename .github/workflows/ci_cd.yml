name: GitHub Actions Build and Deploy
run-name: ${{ github.actor }} runs project 🚀
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Setup Java
        uses: actions/setup-java@v2
        with:
          distribution: 'adopt'
          java-version: '17'

      - name: Run build-and-deploy.sh
        run: |
          /usr/bin/bash -e ./build-and-deploy.sh

      - name: Run jungle-book-backend/build.sh
        run: |
         /usr/bin/bash -e ./jungle-book-backend/build.sh

      - name: Run jungle-book-frontend/build.sh
        run: |
          /usr/bin/bash -e ./jungle-book-frontend/build.sh

      - name: Run k8s/deploy.sh
        run: |
          /usr/vin/bash -e deploy.sg